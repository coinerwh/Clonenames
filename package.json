{
  "name": "codenames_server",
  "version": "1.0.0",
  "description": "Serverside for codenames",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "cd clonenames-client && npm install && npm run build",
    "dev": "nodemon app.js"
  },
  "author": "Wil Coiner",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "socket.io": "^4.0.0"
  }
}
